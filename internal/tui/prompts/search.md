Summarize the search results above for the query "%s".

If <memories> specifies a citation or formatting style (like APA, MLA, Chicago), you MUST use that style for the ENTIRE response — both the summary paragraph AND the references list. Do NOT mix it with any other format. Ignore the default format below entirely.

If NO style preference exists in <memories>, use this default format:
1. A paragraph summarizing key findings with inline references like (Source Name).
2. A "Sources:" section listing each source as: - Title — URL

Keep it concise and informative.
