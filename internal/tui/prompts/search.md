Summarize the search results above for the query "%s".

## Core Rules

**ACCURACY OVER COMPLETENESS.** Only state facts explicitly present in the search results above. If the results don't contain enough information to fully answer the query, say so clearly — NEVER guess, assume, or fill in missing details from your training data.

- Every claim must trace to a specific search result. Do not blend your general knowledge with search data.
- If search results conflict with each other, note the disagreement and present both sides with their sources.
- If the results are tangential (e.g. the query was about X but results mostly cover Y), acknowledge the gap: "The search results primarily cover Y rather than X directly."
- If results contain outdated information (old dates, deprecated versions), flag it: "Note: this information is from [date] and may be outdated."

## Citation Style

If <memories> specifies a citation or formatting style (APA, MLA, Chicago, IEEE, etc.), you MUST use that style for the ENTIRE response — both the summary and the references. Do NOT mix formats. Ignore the default format below entirely.

If NO style preference exists in <memories>, use this default format:

1. **Summary paragraph(s)** — synthesize key findings with inline citations like (Source Name, Year) or (Source Name). Group related findings together rather than summarizing each source separately.

2. **Sources** section — list each source used:
   - Author/Organization. Title. URL

## Response Guidelines

- **For factual queries** ("what is X?", "how does Y work?"): Lead with the direct answer, then provide supporting detail.
- **For comparison queries** ("X vs Y", "best Z for..."): Use a structured comparison — don't just list what each source says separately.
- **For current events** ("latest X", "2026 Y"): Prioritize the most recent sources. Note publication dates when they matter.
- **For how-to queries** ("how to X"): Present steps in order. If multiple sources describe different approaches, note which is most commonly recommended.
- **For price/product queries**: Always note the date and region of pricing data, as it may not apply to the user's location.

Keep it concise and informative. Don't pad with generic introductions like "Based on the search results..." — go straight to the substance.