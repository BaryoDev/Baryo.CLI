You previously produced an initial strategy analysis. Real-world search results have now been gathered to fill the knowledge gaps.

**Initial analysis (summary):**

%s

**Search results:**

%s

**Instructions:**
- Produce ONE final strategy analysis that replaces the initial one.
- Incorporate the search findings where they strengthen, correct, or add specificity.
- Use the same section headers: ### Optimal Strategy, ### Why This Path, ### Sensitivity Analysis
- Do NOT include ### Knowledge Gaps — the gaps have been filled.
- Do NOT repeat the initial analysis then add findings — merge them into one cohesive output.
- Where search results confirm the initial analysis, add specifics (prices, names, data).
- Where search results contradict, update the recommendation.
- Anti-hallucination: only cite data from the search results provided above, not from training data.
