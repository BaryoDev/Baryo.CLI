You previously produced a strategy analysis. The user is providing new information that may affect the recommendation.

CRITICAL ANTI-HALLUCINATION RULES:
- Base your response ONLY on the user's provided facts — do NOT invent data, statistics, product names, prices, or research findings.
- If the new information introduces a topic you don't have data on, say so and suggest a `/search` query.
- Every claim must trace to something the user stated or that appeared in previous search results.

## Previous Strategy Summary

%s

## New Information from User

"%s"

## Your Task

### 1. Assess Impact

Determine whether this new information **materially changes** the strategy. Consider:
- Does it invalidate any assumptions the previous strategy relied on?
- Does it shift the weighting of key factors?
- Does it introduce new constraints or remove existing ones?
- Does it make a previously dismissed option viable (or vice versa)?

### 2. If the Strategy Changes

Show the updated strategy with changes clearly marked:
- Use **CHANGED:** prefix on modified steps or recommendations
- Use **NEW:** prefix on entirely new steps
- Use **REMOVED:** prefix on steps that no longer apply
- Explain what shifted and why — connect the change to the new information
- Keep unchanged parts brief (reference them, don't restate them fully)

### 3. If the Strategy Doesn't Change

Explain specifically why:
- "Your [new fact] doesn't change the strategy because the key drivers are [X] and [Y], and this fact doesn't alter either."
- Don't just say "it doesn't change" — show your reasoning.
- If the information is useful context but doesn't affect the recommendation, say so.

### 4. Updated Knowledge Gaps

If the new information opens new questions, suggest additional `/search` queries. If it answers a previous knowledge gap, note that it's been resolved.

Be direct and concise. Don't restate the entire strategy unless significant parts changed.