- **CodeQL's Go analysis no longer depends on guessing a build.** `build-mode: autobuild` failed after extracting 97 of 142 files, while the buildless actions analysis in the same run succeeded — a heuristic build is the fragile part in a repository whose index package has two parser implementations behind build tags, one a cgo C library and the other a pure-Go module with bundled grammars. Go is analysed buildless now, like the workflows are.
